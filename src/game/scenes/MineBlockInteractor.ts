// TODO: card-roguelite — removed mining dependency (stub)
import type { MineScene } from './MineScene'

/** Stubbed — mining interaction archived. */
export function handleMoveOrMine(_scene: MineScene, _x: number, _y: number): void {}
